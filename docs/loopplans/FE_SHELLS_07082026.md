# Loop plan — FE shells (Public → Auth → Protected)

Living plan for a **scoped** Autopilot / cloud-agent loop.

**Branch lock:** `fe-shells-07082026` only  
**Backup snapshot (do not commit on):** `backup-07082026-1749` @ `51f209d` (pre-shells `master`, 2026-08-07 17:49 IST)  
**Base:** `master` @ `51f209d` (post overnight trust + landing star fix)  
**Do not** open a second PR for this work — update the existing draft PR for this branch  
**Loop interval:** 15–30 minutes (human-armed; not GitHub Actions)  
**This file:** read at the start of every tick; update at the end of every successful tick  
**Created:** 2026-08-07  

Related:

- [`FE_DEPTH_UX_06082026.md`](FE_DEPTH_UX_06082026.md) — LOOP COMPLETE (page molecules + FE→API edges; archaeology)  
- [`SHREE_WHITEBOARD_06082026.md`](SHREE_WHITEBOARD_06082026.md) — Beginner caps / marketing demotion  
- [`WALKABLE_GRAPH_CONTEXT.md`](../WALKABLE_GRAPH_CONTEXT.md) — tier walk  
- Human redesign brief (2026-08-07): scrape census OK; **surface** must become product walk  

**Pitch:**  
Scrape the census; project a walk. Beginner FE is **Public → Auth → Protected** (shells), not a page parade. Nest routes under shells; keep design-system / unused / functions off story tiers until Advanced-in-focus.

**Explicitly later (out of this loop):** product-region Intermediate heuristics, Parts-bin UX polish, AI naming, new language extractors, meeting bots, Graphify-style query MCP.

---

## Redesign principle (read every tick)

> Scrape the census; project a walk: Public → Auth → Protected → Route → (later) Product regions → API; put design-system and unused code in a side bin; never open the function flood until Advanced-in-focus.

---

## Mission (read every tick)

1. **Contract** — encode `access`, `shell`, `surface`, `reachability` (or equivalent metadata) so story vs code vs library vs noise is explicit — not only `featureRoot` / `leafChrome` / score nudges.  
2. **Access signals (Pass A, light)** — App Router groups, `middleware.ts` matchers, layout/session guards, auth config routes — with `observed` / `derived` / `inferred` honesty. **Never invent an Auth wall** from path names alone.  
3. **Shell projection (Pass B)** — Beginner shows shell hubs (+ one hero when possible), not every `page.tsx` as a peer. Auth pages collapse into one Auth gate. Protected pages nest under Protected / App.  
4. **Temp* / marketing** — stay nested or demoted; must not reappear as Beginner peers beside Landing / Sign in / App.  
5. **No AI naming.** Deterministic extract + project only.

**Keep:** full TS scrape, `apis/**` / server-action story edges, `uiOnly`, certainty model, verify floors, self-map + mini-stack regressions.

**Stop / demote:** treating every App Router page as a Beginner hub; auth as ranking bonus only; one-hop `featureRoot` as the *primary* Intermediate model (defer region rewrite to a later loop); more scraper breadth.

---

## Scope of *this* loop

| In | Out |
|----|-----|
| Phase 0 — schema/docs + fixture metadata floors | Phase 2 full product-region Intermediate |
| Phase 1 — access extract + shell hubs + nest pages | Phase 3 Parts-bin UI / orphan badges |
| Beginner narrative: Landing · public peers (capped) · Auth · App/Protected · optional API | Phase 4–5 AI overlay |
| Intermediate: focus shell → **routes only** (grouped/capped) — minimum viable | New extractors / BE Heart work |
| mini-next (+ groups/middleware) verify floors | Meeting products, landing page edits |

If Intermediate “routes only” is unfinished when shells work, prefer finishing shells + Beginner dogfood over region inventiveness.

---

## End goal

| Surface | Done looks like |
|---------|-----------------|
| **Contract** | Nodes/systems that participate in FE story carry `access` / `shell` / `surface` (names may alias existing fields if documented); verify asserts presence on fixture |
| **Fixture** | `mini-next` (or extension) with `(public)` / `(auth)` / `(app)` + middleware (or layout guard) projects **shell Beginner**, not flat page parade |
| **Learn** | Beginner reads like an entrance: Landing/Hero → Sign in (Auth) → App/Protected (± HTTP API). Not 8+ peer page molecules. `temp*` nested under App, not Beginner peers |
| **Scholar** | Same shell grammar or honest fewer shells; no invented Auth wall; no regression to marketing page flood |
| **Viewer** | Double-click App/Protected → Intermediate shows **routes** (calm), not Card/Button/function flood |
| **Honesty** | No `access=protected` without ≥1 strong signal (middleware / layout guard / clear group+guard). Weak name heuristics alone → `unknown` / calm App routes |
| **Verify** | `npm run build` + `npm run verify` green; self-map + mini-stack + prior FE-depth floors unbroken |

### Definition of unfinished

- Beginner still a flat list of page molecules on Learn / fixture  
- Auth is only a score tweak, not a shell hub  
- Protected pages sit on Beginner as peers of Landing  
- Fake Auth wall with no middleware/layout/session evidence  
- Intermediate still opens design-system / function flood on shell focus  
- Next focus empty while End goal unmet  

---

## LOOP COMPLETE — soft stop

Once **Loop status = LOOP COMPLETE**, idle ticks must **not invent, commit, or push**.

### Acceptance gates (all required)

1. **Contract** — Document + encode FE story metadata (`access`, `shell`, `surface`, `reachability` or mapped equivalents). Verify floor on fixture that story hubs expose shell/access.  
2. **Access extract** — At least middleware matcher **or** layout/session guard **or** App Router group convention contributes `access` / evidence on pages or shells; certainty honest. Verify floor.  
3. **Shell Beginner** — Fixture Beginner Product Flow is shells (+ hero), **not** one hub per `page.tsx`. Auth routes collapsed into Auth gate. Verify hub labels/order.  
4. **Nesting** — Protected (or `(app)`) pages are **not** Beginner peers; they appear when focusing Protected/App (Intermediate routes list). Verify.  
5. **No fake wall** — Fixture/case without guards does **not** invent `protected` solely from `/dashboard`-style names. Verify negative floor.  
6. **Intermediate routes-only (MVP)** — Focusing Protected/App does not show leaf chrome / modules / functions as default Intermediate nodes. Verify string/visibility floors.  
7. **Regression** — self-map, mini-stack, prior mini-next FE→API / uiOnly floors still pass; Heart Beginner not regressed by FE projection changes.  
8. **Dogfood** — `npm run verify` every ACTIVE tick. When Shree present: re-scan Learn (required) + Scholar (preferred); Learning log hub list. **Browser Beginner** note before LOOP COMPLETE (`Browser: …`). Cloud agents: fixtures close mid-loop; **final Learn browser/dogfood may be human-local** — do **not** push empty “Shree unavailable” spam.  
9. **Status board** — mandatory items done or cancelled with reason.

### Loop status

```text
LOOP COMPLETE
```

### Idle protocol

Re-check gates; regression → ACTIVE + fix + push; else when complete: `IDLE: LOOP COMPLETE — no push`.  
Human should disable Autopilot when complete.

**Concurrency:** If a previous tick is still running, skip entirely. Never interrupt, rebase, or force-push.

**Backup:** If this loop goes south, reset work from `backup-07082026-1749` (do not commit product changes onto the backup branch).

---

## Dogfood plug (every ACTIVE tick)

```bash
npm run build && npm run verify
```

When Shree exists on this machine:

```bash
node dist/cli.js scan "${SHREE_ROOT:-/Users/praveen/Documents/GitHub/shree}/shree-learn" -o "${SHREE_ROOT:-/Users/praveen/Documents/GitHub/shree}/shree-learn/.underdelta"
node dist/cli.js scan "${SHREE_ROOT:-/Users/praveen/Documents/GitHub/shree}/shree-scholar" -o "${SHREE_ROOT:-/Users/praveen/Documents/GitHub/shree}/shree-scholar/.underdelta"
```

Learning log: Beginner hub labels + whether Auth/Protected shells appeared + Intermediate note on App focus.

---

## Status board

### Done

- [x] Human redesign brief accepted (shells > page parade)  
- [x] Backup branch `backup-07082026-1749` @ `51f209d`  
- [x] Work branch `fe-shells-07082026` created  
- [x] This plan created  
- [x] Phase 0: contract module `src/feShells.ts` + schema metadata docs  
- [x] Phase 0: `verification/mini-next-shells` with `(public)` / `(auth)` / `(app)` + middleware + layout guard  
- [x] Phase 0: extract route-group → `access`/`shell`/`surface`/`reachability` on pages; middleware config node  
- [x] Phase 0 verify floors (access by path + middleware matchers + no-fake-wall on flat mini-next)  
- [x] Phase 1: project shell hubs; nest page molecules; Beginner Home → Auth → Protected  
- [x] Shell Beginner verify floors (hub labels/order + nesting + no shell hubs on flat mini-next)  
- [x] Intermediate MVP: shell focus → routes only (viewer `shellRoutesOnlyVisible`; Protected→Dashboard+Settings; Auth→Login; leafChrome/featureRoot omitted)  
- [x] Honesty: middleware prefixes gate Protected; weak `(dashboard)`/`(admin)` groups alone do not invent `access=protected`  
- [x] Learn dogfood (fixture + next-real-repo): Beginner Home→Auth→Protected; Intermediate routes-only floors green  
- [x] LOOP COMPLETE  

### In progress / next

- Idle — no further invent/commit/push on this loop  

### Seed backlog (optional — only after gates green)

- Product regions (API-touching / product nouns) replacing one-hop featureRoot  
- Reachability closure → `library` / `noise` surfaces + Parts bin  
- Scholar shell pass polish  
- AI label overlay (inferred only)  
- Human-local Shree Learn/Scholar browser pass (optional confirmation)

### Next focus (edit every tick)

> **Next focus:** IDLE — LOOP COMPLETE. Do not invent, commit, or push unless a regression re-opens ACTIVE.

### Learning log

```text
- YYYY-MM-DD HH:MM UTC | Done: … | Next: … | Learned: … | Dogfood: … | Browser: …
```

- 2026-08-07 12:45 UTC | Done: confirmed Intermediate routes-only MVP + honesty floors; closed acceptance gates; LOOP COMPLETE | Next: IDLE | Learned: shell Intermediate filter is parentId routeMolecule-only; middleware startsWith prefixes are the strong Protected signal (org groups alone stay unknown); next-real-repo Beginner Home→Auth→Protected→Pricing→HTTP API→Data | Dogfood: npm run build && npm run verify green; mini-next-shells Protected Intermediate = Protected+Dashboard+Settings; Auth = Auth+Login; next-real-repo shell entrance OK | Browser: deferred human-local (cloud; fixtures closed mid-loop) |
- 2026-08-07 12:35 UTC | Done: Phase 1 shell hubs + nest pages; Beginner Home→Auth→Protected; verify floors | Next: Intermediate routes-only MVP on shell focus | Learned: attachToSystem skips semantic hubs — nest page molecules via parentId/contains; Public collapses behind `/` hero; flat mini-next stays page parade (no invented shells) | Dogfood: npm run verify (pending this tick) | Browser: n/a |
- 2026-08-07 12:25 UTC | Done: Phase 0 contract + mini-next-shells + route-group access extract + middleware node + verify floors | Next: Phase 1 shell hub projection | Learned: parseNextAppFile previously dropped `(group)` — keeping routeGroups is the cheap Pass A signal; flat mini-next /dashboard stays access≠protected (no fake wall); shell Beginner hubs still TODO | Dogfood: npm run verify green | Browser: n/a |
- 2026-08-07 12:19 UTC | Done: plan + backup + branch | Next: Phase 0 contract + fixture | Learned: Pass A scrape is fine; Pass B primary object must become shells not page molecules; backup at backup-07082026-1749 | Dogfood: n/a (plan) | Browser: n/a |

---

## Tick protocol

1. Concurrency skip if busy  
2. Sync on `fe-shells-07082026`; merge `origin/master` when needed; build+verify green  
3. If LOOP COMPLETE → Idle (no push)  
4. One Next focus only  
5. Implement → verify → update plan (Done / Next focus / Learning log) → commit → push  
6. Never commit on `backup-07082026-1749`  
7. No AI naming; no fake Auth walls  
8. Prefer fixture floors over waiting on Shree; final Learn browser may be human-local  

### Copy-paste Autopilot prompt

```text
Underdelta loop: docs/loopplans/FE_SHELLS_07082026.md on branch fe-shells-07082026 only.
Read the plan. Obey redesign principle: shells Public→Auth→Protected; scrape census OK; surface story only.
Do not invent Auth walls without middleware/layout/session/group+guard evidence.
One Next focus. npm run build && npm run verify. Update Learning log. Commit and push this branch.
When LOOP COMPLETE: no invent/commit/push. Never commit on backup-07082026-1749.
```

---

## Projection notes (implementers)

### Shell emission (Pass B)

- Emit **2–3 shells from evidence**, not a forced trinity.  
- No strong protected signal → calm **App routes** / public+auth only — do not fake `protected`.  
- Exactly **one hero** when possible (`/`, marketing home). Cap other public peers (1–2); rest under Public → Marketing group (or omitted from Beginner).  
- Auth signin/signup/forgot/OAuth → **one Auth hub**.  
- `temp*` / marketing exam pages → nested under Protected/Public groups, **never** Beginner peers once shells exist.

### Metadata (Phase 0 target)

Prefer explicit fields on story nodes / synthetic systems:

| Field | Values (sketch) |
|-------|-----------------|
| `access` | `public` \| `auth` \| `protected` \| `unknown` |
| `shell` | `public` \| `auth` \| `protected` (when participating in a shell) |
| `surface` | `story` \| `code` \| `library` \| `noise` |
| `reachability` | `route-tree` \| `orphaned` \| `external-package` |

Map legacy `featureRoot` / `leafChrome` / `collapsedInOverview` into `surface` over time; do not maintain contradictory dual systems forever.

### Access signals (Pass A)

| Signal | Certainty |
|--------|-----------|
| App Router group `(auth)` / `(public)` / `(app)` / … | observed (path) |
| `middleware.ts` matcher + login redirect | observed |
| Layout `getServerSession` / `requireUser` / redirect to login | observed / derived |
| NextAuth / Clerk sign-in route config | observed |
| Only linked from logged-in chrome | derived |
| `/admin` name alone | inferred — **never sole proof** |

---

## Priority

1. Contract + fixture shape  
2. Access extract  
3. Shell Beginner + nesting  
4. Intermediate routes-only MVP  
5. Learn dogfood + Browser  
6. LOOP COMPLETE  
