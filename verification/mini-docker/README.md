# Mini Docker notes

Isolated Docker / Compose verification fixture for Underdelta. Scanned on its
own by `npm run verify`; ignored by a normal product scan of the repo root.

## Containers

Deployable services declared in `docker-compose.yml` plus an app `Dockerfile`.
