# Mini Helm notes

Isolated Helm verification fixture for Underdelta. Scanned on its own by
`npm run verify`; ignored by a normal product scan of the repo root.

## Charts

API and Web Deployments, an API Service, and an Ingress declared as a Helm
chart under `charts/notes/`.
