# Scholar portal

Verification fixture: FE-only Next App Router (no `app/api`, no `src/apis`, no
Prisma/models). Locks Scholar calm + honesty when **no** static server surface
exists — Beginner page hubs stay calm/`uiOnly`, and Underdelta must not invent
API / Data / Jobs neighbors. See `mini-scholar-apis` for the client-`apis/`
OR-branch.

## Product UI

App Router pages for learning surfaces (home, courses, library, profile) plus
marketing/content landings that must not each own a Beginner flow slot when the
page count exceeds the compression cap.
