# Mini Kubernetes notes

Isolated Kubernetes verification fixture for Underdelta. Scanned on its own by
`npm run verify`; ignored by a normal product scan of the repo root.

## Workloads

API and Web Deployments, an API Service, and an Ingress declared under `k8s/`.
