# Mini Vue board

Isolated Vue Router verification fixture for Underdelta. Scanned on its own by
`npm run verify`; ignored by a normal product scan of the repo root.

## Board UI

Vue Router pages (`/` Home, `/dashboard`) declared in `createRouter` with view
component bindings. Beginner should show route molecules, not one UI blob.
