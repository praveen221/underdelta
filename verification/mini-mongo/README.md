# Mini Mongo notes

Isolated Express + Mongoose verification fixture for Underdelta. Scanned on its
own by `npm run verify`; ignored by a normal product scan of the repo root.

## Notes API

HTTP routes for listing and creating notes.

## Catalog data

MongoDB collections declared with Mongoose models (notes, users, tags).
