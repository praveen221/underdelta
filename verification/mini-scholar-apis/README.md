# Scholar portal (client APIs)

Verification fixture: Scholar-shaped Next App Router with `src/apis/**` helpers
that call an **external** Heart backend (no in-repo `app/api`, Prisma, or jobs).

Locks the Scholar honesty OR-branch — when static client APIs resolve, Beginner
gains evidenced page→HTTP API story edges without inventing Data/Jobs, and
without blanket inferred `uses` from every page hub.
