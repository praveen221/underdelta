apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: notes
namePrefix: notes-
# Legacy Kustomize `bases:` (pre-resources) still wires overlay→base needs.
bases:
  - ../../bases/api
  - ../../bases/web
resources:
  - ingress.yaml
