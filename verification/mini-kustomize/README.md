# Mini Kustomize notes

Isolated Kustomize verification fixture for Underdelta. Scanned on its own by
`npm run verify`; ignored by a normal product scan of the repo root.

## Overlays

API and Web Deployments, an API Service, and an Ingress declared as a Kustomize
overlay under `kustomize/notes/`.
