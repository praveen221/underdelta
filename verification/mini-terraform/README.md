# Mini Terraform notes

Isolated Terraform verification fixture for Underdelta. Scanned on its own by
`npm run verify`; ignored by a normal product scan of the repo root.

## Infrastructure

Object storage, API function, catalog table, and a network module declared in
`main.tf`.
